var gamePattern=[];


var buttonColours = ["red", "blue", "green", "yellow"];

var randomChoosenColour 

function nextSequence()
{
    var RandomNumber= Math.floor(Math.random()*4);   //creating random no's b/w 0-3
}

var randomChoosenColour = buttonColours[RandomNumber];   //using randomNumber to select a random colour from the buttonColours array

gamePattern.push(randomChoosenColour);